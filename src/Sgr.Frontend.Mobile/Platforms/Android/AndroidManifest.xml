<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <application android:allowBackup="true"
               android:icon="@mipmap/appicon"
               android:roundIcon="@mipmap/appicon_round"
               android:largeHeap="true"
               android:supportsRtl="true"
               android:usesCleartextTraffic="true" />
  <!--
    largeHeap=true: pide al OS más memoria para la app.
    El intent de la cámara es pesado y, con heap default, Android frecuentemente
    mata el proceso de SGR mientras está la cámara abierta (process death).
    Eso obligaba a Blazor a arrancar en `/` perdiendo el state de PointCreate.
  -->
  <!--
    usesCleartextTraffic=true permite HTTP plano (sin TLS) hacia el backend
    durante desarrollo (localhost vía adb reverse). En producción TLS es obligatorio.
  -->
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.INTERNET" />

  <!-- Geolocalización (US-06 Slice 3.1). FINE = GPS preciso; COARSE = fallback por red/WiFi. -->
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
  <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

  <!-- Cámara (US-06 Slice 3.3). MediaPicker delega en la app de cámara nativa, pero
       declarar el permiso da UX consistente y permite filtrado en Play Store. -->
  <uses-permission android:name="android.permission.CAMERA" />
  <uses-feature android:name="android.hardware.camera" android:required="false" />
</manifest>
